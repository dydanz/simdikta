# Claude Project Guide — Simdikta\n+\n+Last updated: 2026-03-23\n+\n+## Folder layout\n+- `context/project/` — Simdikta-specific rules and facts (architecture, domain lexicon, data safety, regulations, frontend/backend principles, tech stack, overview).\n+- `context/shared/` — Reusable conventions not tied to Simdikta.\n+- `agents/` — Role definitions for helper agents.\n+- `commands/` — Slash-command prompts (governed by Product Research → PRD → TRD → Plan chain).\n+- `templates/` — PRD/TRD skeletons.\n+- `workflows/` — Operational playbooks.\n+- `thoughts/` (external) — Generated research/PRD/TRD/plan outputs per governance.\n+\n+## Governance chain (non-negotiable)\n+Product Research ✅ → PRD ✅ → TRD ✅ → Plan ✅ → Implementation.\n+If any upstream doc is ⚠️ Needs Update, block downstream commands.\n+\n+## Simdikta guardrails (must be enforced by agents/commands)\n+- Multi-tenant safety: `school_id` first arg; SQL must start with `WHERE school_id = $1`.\n+- Data safety: NIK/KK encrypted (AES-256-GCM), masked in responses; no logging of PII.\n+- Audit: `ppdb_audit_logs` append-only; status changes log-before-update in same transaction.\n+- Clean Architecture boundaries: no business logic in handlers; domain imports nothing; services depend on interfaces only.\n+- Selection engine is the riskiest area — prioritize testing it first in plans.\n*** End Patch ***!
