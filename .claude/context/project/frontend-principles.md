# Frontend Principles — Next.js App Router\n+\n+Last updated: 2026-03-23\n+\n+## Defaults\n+- Server Components by default; add `'use client'` only to interactive leaves.\n+- School context comes from `SchoolSlugProvider`; avoid prop drilling beyond 2 levels.\n+- React Query keys must be created via `lib/query/keys.ts` only.\n+\n+## Forms & validation\n+- React Hook Form + Zod schemas define contracts; no manual type assertions or uncontrolled inputs.\n+- Keep form state local; share cross-step state via Zustand stores where needed (wizard flows).\n+\n+## Data fetching\n+- No `useEffect` for fetch; use Server Components or TanStack Query.\n+- Cache boundaries: prefer server data loaders; revalidate as per API cache headers.\n+\n+## File/module structure\n+- Route layout: `app/ppdb/[schoolSlug]/(public|pendaftar|operator)/...`.\n+- Components grouped under `components/ppdb/{public|pendaftar|operator}`.\n+- Tests co-located: `Component.test.tsx` next to component.\n+- Named exports; only pages/layouts use default export.\n+\n+## Styling & UX\n+- Avoid generic boilerplate; follow intentional layout. Prefer design tokens if present.\n+- Provide loading (`loading.tsx`) and error boundaries per route.\n+\n+## Testing\n+- Unit/integration: React Testing Library.\n+- E2E: Playwright for critical flows (registration, verification, selection, payment).\n*** End Patch ***!
