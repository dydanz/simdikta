# Data Safety & Compliance — Simdikta\n+\n+Last updated: 2026-03-23\n+\n+## PII handling\n+- NIK and KK must be encrypted at rest with AES-256-GCM; keys rotated per field when needed.\n+- Mask NIK/KK in any response: `xxxxxx****0001` pattern; never log raw values.\n+- Do not store NIK/KK plaintext anywhere (DB, logs, caches).\n+\n+## Audit invariants\n+- `ppdb_audit_logs` is INSERT-only — no UPDATE/DELETE ever (including superadmin).\n+- Any status change on `ppdb_applicants` must insert an audit log entry **before** updating status, inside the same transaction.\n+\n+## Multi-tenant safety\n+- Every repository method accepts `schoolID uuid.UUID` as first parameter.\n+- Every SQL query against multi-tenant tables must include `WHERE school_id = $1`.\n+- No cross-tenant aggregation or joins.\n+\n+## Redis usage constraints\n+- OTP storage: TTL 5 minutes; clear on successful verification.\n+- Rate limiting: sliding window per school/user.\n+- Queue: Redis List (BLPOP) for notifications — bounded payload size.\n+- Public dashboard cache: TTL 60s; segregate keys per school.\n+\n+## Storage & residency\n+- All user documents stored on S3-compatible storage in ap-southeast-3 (Jakarta) to satisfy UU PDP data residency.\n+- Use presigned uploads; avoid proxying files through API server.\n+\n+## Logging & observability\n+- Do not log PII (NIK/KK). Redact tokens and secrets.\n+- Structured logging via slog; include school_id in log context for multi-tenant traceability.\n*** End Patch ***!
